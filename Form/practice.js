
// ---------------Practice code----------------------



// // let x = 10;
// // let y = 10;
// // let z = typeof 1.2222;

// // document.write(x===y);





// let count = 1;
// reference: while(count < 5){
//     document.write("Hello World!  <br><br>  ");
//     for(let i = 0; i<2; i++){
//         if(count == 3){
//             break reference;
//         }  
//         document.write("Welcome to Pravisht Digital! <br><br> ");

//     }
//     count++;
// }


                   //typecasting

// let type = 1;
// document.write(type);
// let newType = Boolean(type);
// document.write("<br>"+newType);
                   // Strings    
// let string1 = "             I am HRICIAN     ";
// document.write(string1.length); 


              // Arrays
//  let arr = [1, 2, 3, 4, 5];
//  arr.splice(1,0,10);
// document.write(arr);

// for backside adding into array: arr.push();
// for frontside adding into array:  arr.unshift();
// for backside removing from array: arr.pop();
// for frontside removing from array: arr.shift();
// for removing from specific index: arr.splice(index, no. of elements to remove);
// for adding into specific index: arr.splice(index, 0, element to add);
// for replacing into specific index: arr.splice(index, no. of elements to remove, element to add);
// for finding index of element: arr.indexOf(element);
// for finding last index of element: arr.lastIndexOf(element);
// for checking if element is present in array: arr.includes(element);
// for finding length of array: arr.length;
// for sorting array: arr.sort();
// for reversing array: arr.reverse();
// for spliting string into array: str.split(separator);    
// for joining array elements into string: arr.join(separator);
// for slicing array: arr.slice(start index, end index);   
// for splicing array: arr.splice(start index, no. of elements to remove, element to add);
// for concatenating arrays: let newArr = arr1.concat(arr2, arr3, arr4, ...);  
// 


// for iterating array: arr.forEach(function(element, index, array){ // code to execute for each element });
// for mapping array: let newArr = arr.map(function(element, index, array){ // code to execute for each element; return new element; });
// for filtering array: let newArr = arr.filter(function(element, index, array){ // code to execute for each element; return true or false; });
// for reducing array: let result = arr.reduce(function(accumulator, currentValue, index, array){ // code to execute for each element; return new accumulator; }, initialValue);
// for finding element in array: let foundElement = arr.find(function(element, index, array){ // code to execute for each element; return true or false; });
// for finding index of element in array: let foundIndex = arr.findIndex(function(element, index, array){ // code to execute for each element; return true or false; });


// function numberCount(num){
//     for(let i = 1; i<=num; i++){
//         document.write(`${i}<br>`);
//     }
// }

// numberCount(10);